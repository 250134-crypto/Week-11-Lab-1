#include <bits/stdc++.h>
using namespace std;

int main() {
    vector<long long> a;
    long long x;
    while (cin >> x) a.push_back(x);

    for (int i = 1; i + 1 < a.size(); ++i)
        if (a[i] < a[i-1] && a[i] < a[i+1])
            cout << a[i] << ' ';
    cout << '\n';
    return 0;
}
