#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    vector<long long> v;
    for (int i = 0; i < n; ++i) {
        long long x; cin >> x;
        if (x > 0) v.push_back(x);
    }
    sort(v.begin(), v.end());
    for(auto x : v)
        cout << x << ' ';
}
